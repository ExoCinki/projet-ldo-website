<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}LDO -{% endblock %}</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css" 
        integrity="sha512-YWzhKL2whUzgiheMoBFwW8CKV4qpHQAEuvilg9FAn5VJUDwKZZxkJNuGM4XkWuk94WCrrwslk8yWNGmY1EduTA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>

<body style="background:#151a21">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}"><img src="{{ asset('assets/navicon/syndicate.png') }}" alt="LDO" height="64" width="64"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ml-auto">
      <a class="nav-item nav-link active" href="{{ path('home') }}">Home</a>
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
      <a class="nav-item nav-link" href="{{ path('crafter') }}">Les Crafters</a>
      <a class="nav-item nav-link" href="{{ path('leveling_job') }}">Leveling Métiers</a>
      <a class="nav-item nav-link" href="{{ path('gemme') }}">Gemmes</a>
      <a class="nav-item nav-link" href="{{ path('farm') }}">Farming</a>
      {% endif %}
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
      <a class="nav-item nav-link" href="{{ path('profile') }}"><i class="far fa-user"></i> Mon Compte</a>
      {% endif %}
      {% if is_granted('ROLE_ADMIN') %}
      <a class="nav-item nav-link" href="{{ path('admin') }}"><i class="far fa-user"></i> Administration</a>
      {% endif %}
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
      <li class="nav-item m-1">
      <a class="btn btn-danger" href="{{ path('logout') }}">Deconnection</a>
      </li>
      {% else %}
      <li class="nav-item m-1">
      <a class="btn btn-warning text-light" href="{{ path('app_register') }}">Inscription</a>
      </li>
      <li class="nav-item m-1">
      <a class="btn btn-success" href="{{ path('login') }}">Connexion</a>
      </li>
      {% endif %}
      </div>
    </div>
  </div>
</nav>

    <div class="container">
        <div class="row">
            <div class="col">
                {% block body %}{% endblock %}
            </div>
        </div>

    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>